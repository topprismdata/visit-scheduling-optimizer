# 进离店拜访计划优化框架 · 项目宪法（AGENTS.md）

> **项目定位**：快消外勤零售拜访（FMCG Periodic Visit Scheduling）两阶段运筹优化引擎与智能体决策副驾。  
> **文档效力**：本文件是本项目的**唯一最高宪法**与规范标准，所有 AI 编程代理必须 100% 遵守。全项目自治独立，无任何外部跨目录依赖。

---

## 一、唯一权威数据源与实测口径

1. **Layer 1 全月排历唯一数据源**：  
   `/Users/ghb/Downloads/进离店内销售的SRP-7月拜访计划.xlsx`（Sheet1，`计划是否有效标识 == 有效`）。
2. **Layer 2 实测对比数据源**（仅用于实际打卡走法 vs Agent 在途规划验证，严禁反哺排历规划）：  
   `/Users/ghb/Downloads/进离店报表导出 (4).xlsx`。
3. **距离与物理度量衡**：  
   - **唯一合法距离口径**：OpenStreetMap 骑行路网距离（OSM FOSSGIS `routed-bike/table`）。
   - **严禁**：使用 Haversine 直线距离、欧氏距离，或无依据的系数估算。
   - **全量日级缓存**：`data/cache/dist_{line}_{date}_{n}.npy` 已于 2026-09-05 全量重刷完毕，100% 替换为真实路网矩阵（回退缓存已彻底清空归档于 `data/cache/fallback_backup_20260905/`）。
4. **工作日历与周期**：  
   周六日一律剔除，严格以 2026 年 7 月全部 **23 个法定工作日**为基准周期。

---

## 二、业务建模与作业走廊铁律（Business Corridor Constraints）

### 2.1 拜访合同与节拍本体（P0 硬约束 · 2026-09-06 数据考古定稿）
**频次的真实单位是"合同"，月内次数 $f_c$ 是派生量，不是合同本身**。全办 1,524 家店（10 线）的原始计划呈**零例外**的三类合同结构：

| 合同 (cadence) | 相位 φ | 挂 k=4 星期几(周一/二) 月内次数 | 挂 k=5 星期几(周三/四/五) 月内次数 | 店数 |
|---|:---:|:---:|:---:|---:|
| **周访**（每周一次） | — | 4（全部 4 个槽位） | 5（全部 5 个槽位） | 1,337 |
| **双周访·偶相位** | 0 | 2（月内周 1、3） | 2（月内周 2、4） | 88 |
| **双周访·奇相位** | 1 | 2（月内周 2、4） | **3（月内周 1、3、5）** | 99 |

- **不存在"月度 3 次"合同**：数据中 62 家 f=3 店全部是"双周访·奇相位挂在 k=5 星期几"的产物（日期铁证：47 家周三 = 7/01,15,29；9 家周四 = 7/02,16,30；6 家周五 = 7/03,17,31，全部严格隔 2 周）。
- **相位锚定机制（已破译）**：SRP `服务周` 字段 = **ISO 自然周序 mod 4**（7 月：7-01=ISO 周 27→'3'，7-08→'0'，7-15→'1'，7-22→'2'，7-29→'3'，1,524 家全部吻合）。双周相位 = ISO 自然周 mod 2：{0,2} 偶组 / {1,3} 奇组。该锚定**跨月自动延续**（8 月第 1 周 = ISO 32 → 偶组），保证双周店节拍不断链。
- **f 分布口径**：全办 {f=2: 125, f=3: 62, f=4: 557, f=5: 780} 全部可由上表派生；f=4=周访×k4，f=5=周访×k5，f=2=双周×k4 或双周偶相位×k5，f=3=双周奇相位×k5。
- **验收铁律**：每店月内日期集必须精确等于其（合同,相位,σ(c)）决定的槽位集；实现为 `core/metric.py` 合同原语（Phase A 交付，当前仅有均匀节拍松弛版 `check_rhythm`，合法集 ⊃ 合同版，见 `docs/design/CONTRACT_CADENCE_MODEL.md`）。
- **语义演化记录**（防再错）：月频次任意组合 → 周频次均匀节拍 → **合同-相位本体**（最终版）。每次修正均由"拿原始 SRP 计划全量核验零例外"钉死，语义以数据为零例外事实源，禁止臆测。

### 2.2 双向作业负荷走廊 $[K_{\min}, K_{\max}]$（P0 物理与管理红线）
销售代表是物理人，不是无上限运转的运筹质点。原计划各天的门店数天然构成了经企业管理层核准的合理作业走廊。**任何一天的拜访门店数，必须严格落在该业代历史原计划的走廊内**：
$$K_{\min}(l) \le |S_t(l)| \le K_{\max}(l) \quad \forall t \in T$$
- **上限 $K_{\max}$（防过劳红线）**：严禁把门店过度扎堆堆砌到单日（杜绝单日 50~90 店的作弊解，现场物理不可执行）；
- **下限 $K_{\min}$（防闲置红线）**：严禁为了偷算力里程将某天门店抽空（杜绝单日仅 4 店、5 店的畸形解，避免全职员工出工不出力）。
- **验收铁律**：全办 10 位业代各天门店数必须 100% 满足自身走廊（`capacity_ok = True`）：
  - 线02（黄宏妮）：`[33 ~ 36]` 店
  - 线03（欧祖良）：`[17 ~ 34]` 店
  - 线04（马嘉洲）：`[33 ~ 37]` 店
  - 线05（冯秀珍）：`[28 ~ 35]` 店
  - 线06（梁炯棠）：`[23 ~ 35]` 店
  - 线07（赵成毅）：`[22 ~ 30]` 店
  - 线08（邝豪杰）：`[25 ~ 34]` 店
  - 线09（梁健满）：`[23 ~ 35]` 店
  - 线10（黄志成）：`[25 ~ 33]` 店
  - 线11（苏泳江）：`[15 ~ 21]` 店

### 2.3 星期几一致性 R2′（P0 客户约定红线 · 2026-09-05 用户澄清定稿）
**语义**：门店**可以**从周一改到周二（优化允许换星期几，否则规划失去意义），但**改了就全月一致**——原第 1/2/3/4 周都在周二的店，新方案第 1~4 次拜访必须全部落在周二，严禁"第 1 周周二、第 3 周周四"式分裂。
$$\forall c \in N:\quad \left| \{ \mathrm{weekday}(t) : t \in S(c) \} \right| = 1$$
- 星期几本身可换（σ: store→weekday 为决策变量），每个星期几 w 有 $k_w$ 个日期槽位（7 月：周一/二=4 个，周三/四/五=5 个）；
- **与合同本体的联合推论（2026-09-06）**：换星期几后月内次数由合同派生——周访店 k4↔k5 换挡 f 随之 4↔5；双周偶相位两挡都是 2；**双周奇相位 k5→k4 换挡 f 3→2**。旧口径 `count_ok`（月内行数硬守恒）与"任意换星期几"在这一点上**数学冲突**，双周奇相位店禁止跨槽位类换挡（待 Phase C 复核是否放开）；
- **验收铁律**：`check_r2prime(days)` 必须返回空（`r2_ok = True`）。旧版"完全锁死原星期几"与"任意跨日移动（允许同店分裂）"**都不是主线契约**；
- **主线基线** = 原分配 + CP-SAT 日内最优排序（天然满足 R2′）；R2′ 的合法自由度 = **整店换星期几（同槽位类内自由，跨类受限见上）+ 相位组内槽位轮换**，列生成与 SP 主问题（z[c,w] 线性化）双侧实现。
---

## 三、标准两阶段架构与算法机制规范命名

全系统严格遵循**两阶段运筹解耦（Two-Stage Decomposition）**，并**全面废除 V1/V3/V4 等无业务含义代码**，统一采用机制驱动命名：

```
┌────────────────────────────────────────────────────────────────────────┐
│ Layer 1: 全月排日历 (Calendar Planning / 跨日分配)                     │
│ 业务红线: 频次守恒 count_ok + 双向作业走廊 capacity_ok [K_min, K_max]    │
│ 终极引擎: 对偶闭环列生成 (Dual-Feedback Closed-Loop CG / SP+CG)         │
│ 生成器池: 路径反馈大邻域搜索 (Feedback ALNS) + 路径反馈遗传 (Feedback HGS)│
└──────────────────────────────────┬─────────────────────────────────────┘
                                   │ 产出合规单日门店集合 S_t
                                   ▼
┌────────────────────────────────────────────────────────────────────────┐
│ Layer 2: 单日排顺序 (Intra-day Routing / 单日路径排序)                 │
│ 业务红线: 开放无场站哈密顿链, 纯真实骑行路网距离                       │
│ 终极引擎: 约束规划全局精确求解器 (CP-SAT Global Exact Circuit Solver)   │
│ 在途辅助: 最近邻局部搜索 (NN+2-opt, 仅用于手机在途毫秒插单)            │
└────────────────────────────────────────────────────────────────────────┘
```

### 3.1 规范命名与技术特征映射

| 规范全称 | 核心机制与特征 | 适用层级 | 业务定位与作用 |
|---|---|---|---|
| **冷评估大邻域搜索**<br/>*(原 ALNS v1)* | **把 TSP 当秤**：无路径携带，每次候选移动冷启动重排全天，算力开销极大，吞吐极低 | Layer 1 | 无反馈基线对照组 |
| **路径反馈大邻域搜索**<br/>*(原 ALNS v3)* | **把 TSP 当眼睛**：携带活跃路径，最差边定向拆除 + $O(n)$ 增量修复 + 欠载日优先补足 + SA 退火 | Layer 1 | **全月规划主力列生成器** |
| **无感知遗传搜索**<br/>*(原 Blind HGS)* | **盲目交叉**：日分配层面随机重排，无路径几何信息指导，易陷入深谷停滞 | Layer 1 | 无反馈遗传对照组 |
| **路径反馈混合遗传搜索**<br/>*(原 Feedback HGS)* | **顺路微链保护**：保留优良单日微链结构，欠载日优先修复，脏日定向短促退火 | Layer 1 | 种群多样性列生成器 |
| **一次性静态集合划分**<br/>*(原 Static SP)* | **一次性组装**：在静态池上求解整数规划，无后续对偶定价反馈 | Layer 1 | 静态组合基线 |
| **对偶闭环列生成**<br/>*(原 SP+CG / SDR)* | **经济学闭环**：LP 对偶价引导启发式定价回灌负约简成本列，CP-SAT 解受限主问题 | Layer 1 | **全月排历终解器**（签发受限池内差距，**非全局认证**） |
| **多目标帕累托稳定器**<br/>*(原 MO-ALNS / v4)* | **锚定历史计划**：在里程、改动量（稳定性）与工作量均衡间求帕累托前沿 | Layer 1.5 | 客户定制业务平衡器 |
| **约束规划全局精确求解器**<br/>*(原 CP-SAT)* | **单日路径唯一引擎**：AddCircuit 建模；**[实测样本] 09 线 4 点集 (n≤35) 均签发 OPTIMAL (22ms~1.06s)**；全线全日型延迟分布复测在路线图，未复测前不外推 | Layer 2 | **单日排线唯一终极引擎** |
| **变深度局部搜索**<br/>*(原 LKH-3)* | **变深度链反转**：在大规模欧氏对称图强，但在城市非对称非欧路网上退化 | Layer 2 | 超大规模单日长路线备用 |
| **最近邻局部搜索**<br/>*(原 NN+2opt)* | **瞬时贪心启航**：< 1 ms 极速出解，但容易受困于局部死胡同 | Layer 2 | 手机端在途实时副驾 |

---

## 四、全办 10 位业代走廊 + 合同-相位严格口径终账（2026-09-06 正式账）

**基线 A（唯一比较基准）**：保持原计划门店→日分配、仅将每日顺序解至 CP-SAT 数学最优后的路网里程（全办 4,144.3 km）。SRP 打印序里程（16,857 km）**禁止用作降幅分母**（评审 P2-6）。
**终解口径（正式账）**：合同槽位集原生列池（`r2_alns` `combo_mode=contract`，确定性迭代预算）+ `SP(contract)` 义务覆盖重组，**10/10 线零分裂、零越界、零合同违约、结构保证成立、终局重排 230/230 天 PROVEN OPTIMAL**。Gap 只称"受限池内差距"（评审 P1-1）。
**数据源**：`output/sp_contract_ledger_all.json` + `output/sp_days_contract_*.json`（规范 day-set）；自由普查对照 `output/contract_audit_r2_ledger.json`。

| 线路 | 业代 | 走廊 | 基线A (km) | 合同终解 (km) | vs基线A | 换店数 | 违约 |
|:---:|:---:|:---:|---:|---:|---:|---:|:---:|
| 02 | 黄宏妮 | [33~36] | 263.3 | **259.0** | −1.63% | 29 | 0 ✓ |
| 03 | 欧祖良 | [17~34] | 220.8 | **163.6** | −25.92% | 106 | 0 ✓ |
| 04 | 马嘉洲 | [33~37] | 261.8 | **227.3** | −13.18% | 31 | 0 ✓ |
| 05 | 冯秀珍 | [28~35] | 388.8 | **284.7** | −26.76% | 109 | 0 ✓ |
| 06 | 梁炯棠 | [23~35] | 157.9 | **117.1** | −25.81% | 116 | 0 ✓ |
| 07 | 赵成毅 | [22~30] | 371.8 | **346.3** | −6.86% | 89 | 0 ✓ |
| 08 | 邝豪杰 | [25~34] | 807.0 | **620.6** | −23.10% | 52 | 0 ✓ |
| 09 | 梁健满 | [23~35] | 326.6 | **316.3** | −3.15% | 7 | 0 ✓ |
| 10 | 黄志成 | [25~33] | 386.3 | **386.3** | 0.00% | 1 | 0 ✓ |
| 11 | 苏泳江 | [15~21] | 960.0 | **901.1** | −6.13% | 13 | 0 ✓ |
| **Σ** | **10 人全办** | — | **4,144.3** | **3,622.3** | **−12.60%（净省 522.0 km/月）** | **553** | **0** |

**与旧松弛口径的关系（诚实声明）**：旧账 3,618.5 km（−12.69%）为历史参照、**不可比**——污染普查实证其解类含 111 处合同违约（`output/contract_audit_r2_ledger.json`，重灾区线 08：46/92 双周店违约）。合同严格口径以 **+3.8 km（+0.10%）** 的代价取得全部合法性与终局最优凭证（230/230 天），且换店自由度反而更大（553 vs 168 家，因次数随 σ 派生不再被行数锁死）。旧账仅在引用历史时标注"相位松弛口径"。

**管理规律（2026-09-06 修订，推翻旧结论）**：旧"均衡线(07/10)≈零空间"规律**被推翻**——线 07 在合同口径下 −6.86%（89 家换店）。新规律：**合同空间普遍存在**，10 线中 9 线找到改善（0%~26.8%），唯线 10 为 never-leave-init 地板情形（确定性搜索未找到更优指派，诚实报告为 0）。全部空间来自合同合法的星期几再平衡与相位保持轮换，零牺牲可执行性。

---

## 五、项目工程与研发铁律（Iron Rules）

1. **零外部跨库依赖**：项目全生命周期代码、文档、配置均保存在本仓库内，严禁将项目资产或过程日志外抛至任何用户主目录或共享云盘。
2. **三层设计红线**：所有需求与改动必须严格遵循**业务建模 → 数学方程 → 求解器实现**顺序，严禁直接在求解器打补丁颠倒层级。
3. **物理与约定可行性一票否决**（五道闸口逐线机器核验）：
   - `count_ok`：客户频次 100% 守恒（合同口径下升级为派生次数守恒，见 §2.1）；
   - `capacity_ok`：单日门店数 ∈ $[K_{\min}, K_{\max}]$（防过劳/防闲置双向走廊）；
   - `r2_ok`：每店全月单一星期几（`check_r2prime` 返回空），换店必须整店全月一致；
   - `contract_ok`：每店月内日期集 =（合同, 相位, σ）决定的槽位集（`core/contract.py::check_contract`，2026-09-06 已实现；Task 1 语义层 + Task 2 SP 合同模式 + Task 3 算法层接线；现行 `check_rhythm` 为均匀节拍松弛快筛，只能证伪不能证真）；
   - **结构保证**：SP 终解 ≤ 池内最佳单解 ≤ 基线 A（基线 A 列强制入池，由构造保证）。
4. **命名规范铁律**：严禁在对外报告、代码类名与业务交流中使用 V1/V3/V4 等无含义代号，必须使用标准机制名称。
5. **对账与复现纪律**：所有汇报里程与耗时数字必须取自 `output/bench_*.csv` 或 `output/sp_all_lines_summary.json`，严禁引用未跑出凭证的旧数据。
6. **历史资产保护**：`algos/pvrp_cg/` 为早期基线目录，永久**冻结零改动**。

---

## 六、项目文档结构规范（Google 级标准体系）

本项目文档严格按照**受众与目标职责**四层解耦，根目录杜绝堆积杂文：

```
docs/
├── README.md                      # [导航中心] 全局文档导航地图与评审人定向阅读指引
├── design/                        # [架构蓝图] Google 级系统设计主文档与核心算法 RFC
│   ├── SYSTEM_DESIGN_DOC.md       # ★ 系统架构设计主规范 (Master Spec)
│   ├── CONTRACT_CADENCE_MODEL.md  # ★ 拜访合同-节拍-相位本体 (2026-09-06 数据考古定稿)
│   ├── REPLAN_2026-09-06.md       # ★ 合同本体重设计划 (现状盘点 + Phase A~D)
│   ├── SP_MATHEURISTIC_DESIGN.md  # 集合划分与对偶闭环列生成设计
│   └── V4_PARETO_STABILIZER_DESIGN.md # 多目标帕累托稳定器设计
├── benchmarks/                    # [实证总账] 评测基准与实证报告
│   ├── TWO_STAGE_BENCHMARK_REPORT.md  # ★ 两阶段全景帕累托基准报告 (单日+月度+消融+总账)
│   ├── PERFORMANCE_BENCHMARK.md       # 延迟与响应时间 SLA 生产标定
│   ├── ACTUAL_VS_AGENT_REPORT.md      # Layer 2 现场实际打卡 vs Agent 插单实测报告
│   └── MANUAL_10_DAYS_AUDIT.md        # 10 天人工白盒打卡抽查审计报告
├── guides/                        # [技术专著] 操作手册与算法指南
│   ├── ALGORITHM_GUIDE.md             # 算法机制技术指南 (20+ 篇顶刊 DOI 认证)
│   └── AGENTIC_DISPATCH_GUIDE.md      # Layer 2 现场动态调度副驾技术专著
└── archive/                       # [历史封存] 早期阶段设计、演化草案与废弃方案归档
```
所有新建设计文档必须遵循 `docs/design/SYSTEM_DESIGN_DOC.md` 的 Google 级标准，包含 Goals/Non-Goals、Invariants、Alternatives Considered 等核心章节。
